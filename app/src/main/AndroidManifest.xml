<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.hpnotebook.volunteerapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activities.SplashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activities.LoginActivity" />
        <activity android:name=".Activities.SignupActivity" />
        <activity android:name=".Activities.SearchEventsActivity" />
        <activity android:name=".Activities.EventsNearbyActivity" />
        <activity android:name=".Activities.EventDetailActivity" />
        <activity android:name=".Activities.DashboardActivity" />
        <activity android:name=".Activities.AddEventActivity" />
        <activity android:name=".Activities.UserProfileActivity" />
        <activity android:name=".Activities.PermissionActivity" />
        <activity android:name=".Activities.SocialSigninActivity" />
        <activity android:name=".Activities.CommentsActivity"></activity>
    </application>

</manifest>